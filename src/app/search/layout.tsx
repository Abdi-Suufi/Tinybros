export const metadata = {
  title: 'Search | TinyBros',
  description: 'Search for movies and TV shows on TinyBros',
}

export default function SearchLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children;
}
