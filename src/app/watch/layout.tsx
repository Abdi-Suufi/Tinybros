export const metadata = {
  title: 'Watch | TinyBros',
  description: 'Watch movies and TV shows on TinyBros',
}

export default function WatchLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children;
}
