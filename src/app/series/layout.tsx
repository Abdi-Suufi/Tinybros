export const metadata = {
  title: 'Series | TinyBros',
  description: 'Browse TV series on TinyBros',
}

export default function SeriesLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children;
}
